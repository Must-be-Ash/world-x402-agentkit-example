# Allowlisted wallet — this one is on the VIP list (free access, no funds needed)
ALLOWLISTED_WALLET_ADDRESS=
ALLOWLISTED_WALLET_PRIVATE_KEY=

# Non-allowlisted wallet — this one must pay (needs ETH + USDC on Base Sepolia)
NON_ALLOWLISTED_WALLET_ADDRESS=
NON_ALLOWLISTED_WALLET_PRIVATE_KEY=

# Unfunded wallet — not on the allowlist AND has no USDC (can't pay, will get 402)
UNFUNDED_WALLET_ADDRESS=
UNFUNDED_WALLET_PRIVATE_KEY=

# Allowlist — comma-separated wallet addresses (case-insensitive)
ALLOWLIST_ADDRESSES=
